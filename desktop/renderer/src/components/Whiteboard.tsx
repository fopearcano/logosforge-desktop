import React from 'react';

/** Central blank placeholder area — the real editor arrives in a later milestone. */
export function WhiteboardPlaceholder() {
  return (
    <main className="whiteboard">
      <div className="sheet">
        <p className="placeholder-hint">Your whiteboard will appear here.</p>
      </div>
    </main>
  );
}
