# LogosForge Whiteboard — backend runtime dependencies
# Python 3.10+ (developed/tested on 3.13 — see backend/.python-version).
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
# pydantic-core ships compiled wheels; 2.9 is the first release with Python 3.13
# wheels, so floor it there to guarantee a clean install on the updated Python.
pydantic>=2.9,<3.0

# --- test / development only (installed together for convenience) ---
pytest>=8.0,<9.0
httpx>=0.27
